# IronRift

IronRift is a mobile port of Ironwail

Build from the repository root:

- PowerShell: `scripts/build.ps1` or `scripts/build.ps1 Install`
- Shell: `scripts/build.sh` or `scripts/build.sh Install`
