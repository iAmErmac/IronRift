# Ironwail submodule placeholder

This directory is reserved for the Ironwail submodule. Initialize it with:

    git submodule update --init --recursive

The scaffold currently builds the placeholder IronRift GLES target until Ironwail's Android backend is connected.
